"""
__init__.py file treats the functions created in another .py files 
like a library/package. So we could import them to any other python file.

But, after python 3.3 version it is not necessary to specify 
the __init__.py file, python implicitly imports the functions from another .py files.
"""